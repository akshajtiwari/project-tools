# this is the file that every project should have to run the library , this template remains the same 

[python]
min_version = "3.10"

[dependencies]
type = "requirements"
path = "requirements.txt"

[tools]
required = ["git", "seaborn"]

[env]
required = ["DATABASE_URL", "SECRET_KEY"]

[init]
venv = true
test_command = "pytest"
